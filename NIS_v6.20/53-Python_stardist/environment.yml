name: stardist_cpu_only_3_12

channels:
  - conda-forge
dependencies:
  - python=3.12
  - pip
  - pip:
    - tensorflow
    - stardist
    - numpy==1.26

# conda env create -f environment.yml
# CPU only, as python 3.12 is not supported with tensorflow GPU on windows
